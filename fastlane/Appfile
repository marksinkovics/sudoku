fastlane_require 'dotenv'

before_all do
  Dotenv.overload '.env.secret'
end

app_identifier("com.atakamastudio.Sudoku")
apple_id(ENV['APPLE_ID'])
itc_team_id(ENV['ITC_TEAM_ID'])
team_id(ENV['TEAM_ID'])